#ifndef CALCULATIONHEADER
#define CALCULATIONHEADER
#include <iostream>
#include <string>
using namespace std;

float add(float x, float y);
float substract(float x, float y);
float multiply(float x, float y);
float divide(float x, float y);
void Calculator();

float calculateFieldOfACircle(float radius);

#endif // !CALCULATIONHEADER